patch:
  # 显示为双拼编码，即清空 preedit_format 中的内容
  translator/preedit_format: []
  engine/processors:
    - lua_processor@select_character      # 以词定字
    - recognizer
    - key_binder
    - speller
    - punctuator
    - selector
    - navigator
    - express_editor
