## install stow
in macos
```bash
brew install stow
```
in ubuntu/debain
```bash
sudo apt install stow
```

## use
```bash
git clone https://github.com/AlanLang/dotfile.git ~/dotfiles
cd ~/dotfiles
stow git
```
